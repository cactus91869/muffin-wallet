apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: {{ .Values.externalDb.name }}
  namespace: {{ .Values.namespace }}
spec:
  host: host.minikube.internal
  trafficPolicy:
    tls:
      mode: {{ .Values.externalDb.tls.mode }}
